info:
  name: List Projects
  type: http
  seq: 1
  tags:
    - projects

http:
  method: GET
  url: "{{baseUrl}}/api/v1/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/projects"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "projects": []
          }

docs: List registered project module names (from beampipe.projects entry points).
