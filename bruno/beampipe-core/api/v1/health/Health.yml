info:
  name: Health
  type: http
  seq: 1
  tags:
    - health

http:
  method: GET
  url: "{{baseUrl}}/api/v1/health"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/health"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "status": "",
            "environment": "",
            "version": "",
            "timestamp": ""
          }
